spring:
  datasource:
    url: jdbc:h2:mem:openzev-test;MODE=PostgreSQL;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driverClassName: org.h2.Driver
  h2:
    console.enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true

openzev:
  ckw:
    base-url: http://localhost:8118
  qr-generator:
    base-url: http://localhost:8118
