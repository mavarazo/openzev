Id:
  type: string
  format: uuid

Date:
  type: string
  format: date

DateTime:
  type: string
  format: date-time

ErrorDto:
  type: object
  required:
    - code
    - message
  properties:
    code:
      type: string
    message:
      type: string